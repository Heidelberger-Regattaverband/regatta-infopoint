<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">

  <Table id="timestripTable" autoPopinMode="true" items="{path:'timestrip>/',sorter:[{path:'time',descending:true}]}" growing="true"
    growingScrollToLoad="true" growingThreshold="30" mode="SingleSelectMaster" itemPress=".onSelectionChange"
    sticky="HeaderToolbar,ColumnHeaders" class="sapUiNoContentPadding">

    <!-- The HeaderToolbar shows a title and the filter button -->
    <headerToolbar>
      <OverflowToolbar>
        <content>
          <ToolbarSpacer />
          <!-- Refresh button -->
          <Button text="{=!${device>/system/phone}?${i18n>common.refresh}:''}" tooltip="{i18n>common.refresh}" icon="sap-icon://refresh"
            press=".onRefreshButtonPress" />
        </content>
      </OverflowToolbar>
    </headerToolbar>

    <columns>
      <Column id="timestampTime" hAlign="Center" width="11rem" importance="High" popinDisplay="Inline">
        <Text text="{i18n>common.time}" class="bold" />
      </Column>
      <Column id="timestampSplit" hAlign="Center" width="5rem" importance="High" popinDisplay="Inline">
        <Text text="{i18n>common.split}" class="bold" />
      </Column>
      <Column id="timestampHeatNr" hAlign="Center" width="4rem" importance="Medium" popinDisplay="Inline">
        <Text text="{i18n>common.heatNr}" class="bold" />
      </Column>
      <Column id="timestampBib" hAlign="Center" width="4rem" importance="Medium" popinDisplay="Inline">
        <Text text="{i18n>common.bib}" class="bold" />
      </Column>
      <Column id="timestampStatus" hAlign="Center" width="5rem" importance="Low" popinDisplay="Inline">
        <Text text="{i18n>common.state}" class="bold" />
      </Column>
      <Column id="timestampDelete" hAlign="Center" width="4rem" importance="Low" popinDisplay="Inline">
        <Text text="{i18n>admin.column.actions}" class="bold" />
      </Column>
    </columns>

    <items>
      <ColumnListItem>
        <cells>
          <Text text="{path:'timestrip>time',formatter:'.formatter.timestamp'}" />
          <Text text="{timestrip>split}" />
          <Text text="{timestrip>heat_nr}" />
          <Text text="{timestrip>bib}" />
          <core:Icon src="{=${timestrip>persisted}?'sap-icon://SAP-icons-TNT/data-store':''}" tooltip="{i18n>common.persisted}" />
          <Button icon="sap-icon://delete" tooltip="{i18n>common.delete}" type="Reject" press=".onDeleteTimestamp" />
        </cells>
      </ColumnListItem>
    </items>
  </Table>
</core:FragmentDefinition>