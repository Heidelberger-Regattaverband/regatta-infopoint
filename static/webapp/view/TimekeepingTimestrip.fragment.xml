<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">

  <Table id="timestripTable" autoPopinMode="true" items="{path:'timestrip>/'}" growing="true" growingScrollToLoad="true"
    growingThreshold="30" mode="SingleSelectMaster" itemPress=".onSelectionChange" sticky="HeaderToolbar,ColumnHeaders"
    class="sapUiNoContentPadding">

    <!-- The HeaderToolbar shows a title and the filter button -->
    <headerToolbar>
      <OverflowToolbar>
        <content>
          <ToolbarSpacer />
          <!-- Refresh button -->
          <Button text="{=!${device>/system/phone}?${i18n>common.refresh}:''}" tooltip="{i18n>common.refresh}" icon="sap-icon://refresh"
            press=".onRefreshButtonPress" />
        </content>
      </OverflowToolbar>
    </headerToolbar>

    <columns>
      <Column hAlign="Center" width="11rem" importance="High" popinDisplay="Inline">
        <Text text="{i18n>common.time}" class="bold" />
      </Column>
      <Column hAlign="Center" width="5rem" importance="High" popinDisplay="Inline">
        <Text text="{i18n>common.split}" class="bold" />
      </Column>
      <Column hAlign="Center" width="4rem" importance="Medium" popinDisplay="Inline">
        <Text text="{i18n>common.heatNr}" class="bold" />
      </Column>
      <Column hAlign="Center" width="4rem" importance="Medium" popinDisplay="Inline">
        <Text text="{i18n>common.bib}" class="bold" />
      </Column>
      <Column hAlign="Center" width="5rem" importance="Low" popinDisplay="Inline">
        <Text text="{i18n>common.state}" class="bold" />
      </Column>
      <Column hAlign="Center" width="4rem" importance="Low" popinDisplay="Inline">
        <Text text="{i18n>admin.column.actions}" class="bold" />
      </Column>
    </columns>

    <items>
      <ColumnListItem>
        <cells>
          <Text text="{path:'timestrip>time',formatter:'.formatter.timestamp'}" />
          <Text text="{timestrip>split}" />
          <ComboBox value="{timestrip>heat_nr}" items="{heats>/}" loadItems=".onLoadHeats" change=".onHeatChange">
            <core:ListItem key="{heats>number}" text="{heats>number}" />
          </ComboBox>
          <ComboBox value="{timestrip>bib}" items="{bibs>/}" loadItems=".onLoadBibs" change=".onBibChange">
            <core:ListItem key="{bibs>number}" text="{bibs>number}" />
          </ComboBox>
          <core:Icon src="{=${timestrip>persisted}?'sap-icon://SAP-icons-TNT/data-store':''}" tooltip="{i18n>common.persisted}" />
          <Button icon="sap-icon://delete" tooltip="{i18n>common.delete}" type="Reject" press=".onDeleteTimestamp" />
        </cells>
      </ColumnListItem>
    </items>
  </Table>
</core:FragmentDefinition>